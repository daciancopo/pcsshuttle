import { Baloo_2, Manrope, Quicksand } from 'next/font/google';

export const baloo = Baloo_2({ subsets: ['latin'], weight: ['700','800'] });
export const manrope = Manrope({ subsets: ['latin'], weight: ['600','700'] });
export const quicksand = Quicksand({ subsets: ['latin'], weight: ['600','700'] });